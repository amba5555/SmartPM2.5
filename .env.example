# Centralized environment file for local development and one-place copy/paste
# WARNING: This file contains sensitive secrets. Do NOT commit it to a public repo.
# Copy this file to .env and fill in your actual values

############# Supabase (shared) #############
# Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co
# Supabase service key or anon key (use anon for client-side; service key for backend)
SUPABASE_KEY=your_supabase_anon_key_here

############# Backend API (FastAPI / Render) #############
# Public backend base URL (used by frontend to call API)
VITE_BACKEND_URL=https://your-backend.onrender.com

# Optional backend runtime secrets (for local dev)
BACKEND_DATABASE_URL=postgresql://user:password@localhost:5432/smartpm25_db
BACKEND_SECRET_KEY=change_this_to_a_secure_value

############# Frontend (Vite / SvelteKit) #############
# Vite exports env vars that start with VITE_
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_KEY}

############# MQTT (firmware -> backend ingestion) #############
MQTT_BROKER=your-cluster-id.s1.eu.hivemq.cloud
MQTT_USERNAME=your_hivemq_username
MQTT_PASSWORD=your_hivemq_password

############# Optional Integrations / Feature Flags #############
# Enable server-side aggregation (if enabled in backend)
ENABLE_DB_AGGREGATION=true
# Toggle realtime client behaviour (frontend): live mode subscribes to Supabase
ENABLE_REALTIME=true