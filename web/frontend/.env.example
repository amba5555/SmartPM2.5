# Frontend environment variables
# NOTE: For simplified setup, use the root .env file instead of this one
# The root .env contains all VITE_* variables needed for frontend builds

# If you prefer frontend-specific env file, copy this to .env.local:

# Backend API URL
VITE_BACKEND_URL=https://your-backend.onrender.com

# Supabase Configuration (for realtime subscriptions)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key