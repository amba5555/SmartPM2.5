# MQTT Broker Configuration (HiveMQ Cloud)
MQTT_BROKER_URL=mqtts://your-hivemq-cluster.s2.eu.hivemq.cloud:8883
MQTT_USERNAME=your_hivemq_username
MQTT_PASSWORD=your_hivemq_password
MQTT_TOPIC=smartpm25.sensor.data

# Backend Configuration (Render)
BACKEND_URL=https://smartpm2-5.onrender.com
PING_ENDPOINT=/api/readings/latest

# Optional: Authentication token for wake endpoint
# WAKE_TOKEN=your_secret_token_here

# Rate Limiting Configuration
DEBOUNCE_MS=30000          # Don't ping more than once per 30 seconds per device
MAX_PING_RETRIES=3         # Number of retry attempts for failed pings

# Logging Level (optional)
# LOG_LEVEL=info